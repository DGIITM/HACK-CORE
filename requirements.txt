# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Config / validation
# pydantic must stay >=2.12.5 for google-genai==2.17.0 (see M4/LLM section
# below) — found via a real fresh-install failure when writing
# setup_and_run.py; the venvs built up over development had pip silently
# resolve a newer pydantic than this file declared, so `pip install -r
# requirements.txt` from scratch was actually broken until this was pinned
# to match what was really running.
pydantic==2.13.4
pydantic-settings==2.6.1
python-dotenv==1.0.1

# M4 retrieval + LLM
qdrant-client==1.19.0
scikit-learn==1.9.0
google-genai==2.17.0          # Gemini Developer API (primary) + Vertex AI (fallback), unified client
google-cloud-aiplatform==1.163.0  # kept for Vertex AI ADC/auth plumbing used by the fallback tier

# M7 persistence
google-cloud-firestore==2.28.1

# M1 voice input
google-cloud-speech==2.40.0

# M5 translation + text-to-speech
google-cloud-translate==3.27.0
google-cloud-texttospeech==2.37.0

# M8 causal model
pandas==3.0.5
statsmodels==0.14.6

# Testing
pytest==8.3.4
httpx==0.28.1
